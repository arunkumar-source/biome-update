{
  "name": "new-project",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "generate:api": "openapi-typescript http://localhost:4000/api/openapi -o packages/shared/types/api.ts",
    "check": "ultracite check",
    "check:biome": "biome check --write",
    "fix": "ultracite fix",
    "format": "biome format --write",
    "lint": "biome lint --write",
    "clean:types": "ts-unused-exports tsconfig.json --allowUnusedEnums",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.3.0",
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "husky": "^9.1.7",
    "prettier": "^3.8.1",
    "turbo": "^2.8.1",
    "typescript": "5.9.3",
    "ultracite": "7.2.3"
  },
  "packageManager": "pnpm@10.4.1",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "openapi-react-query": "^0.5.4"
  }
}
